name: daco_login
description: Login UI for Daco Technologies


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts:
  flutter_svg: ^1.0.3
  dio:
  bloc:
  flutter_bloc:
  dartz:
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner:
  json_serializable:

flutter:
  uses-material-design: true
  assets:
    - assets/static/
